# Application Port (automatically detected if not set)
PORT=3847

# Expo Packager Port
EXPO_PACKAGER_PORT=19000

# Expo DevTools Port
EXPO_DEVTOOLS_PORT=19001

# Docker Host IP (for mobile device connection)
DOCKER_HOST_IP=host.docker.internal

# Environment
NODE_ENV=development